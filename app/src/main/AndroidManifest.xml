<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BibleApplication"
        android:label="@string/app_name"
        android:theme="@style/EInkTheme"
        android:hardwareAccelerated="false">

        <activity
            android:name=".TranslationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".BookListActivity" />
        <activity android:name=".ChapterListActivity" />
        <activity android:name=".VerseActivity" />
        <activity android:name=".DownloadLanguagesActivity" />
        <activity android:name=".DownloadModulesActivity" />

    </application>

</manifest>
